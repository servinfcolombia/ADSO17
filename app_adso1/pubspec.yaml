name: app_adso1
description: Aplicación de login con FastAPI y Flutter.

publish_to: 'none' # Evita publicar accidentalmente en pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true